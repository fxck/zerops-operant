<!-- #ZEROPS_EXTRACT_START:intro# -->
Run Operant on a single node: managed Postgres plus the control plane, OpenClaw
gateway and sandbox Docker host each on one container. The simplest, cheapest way to
self-host Operant for a team — ideal for evaluation and steady, low-to-moderate
traffic. There's no redundancy, so a container restart briefly drops chat ingress;
step up to HA Production for higher availability.
<!-- #ZEROPS_EXTRACT_END:intro# -->
